while True:
    l="login"
    p=122333
    name="Tom Holland"
    a,b=input("Login:\n"),input("Password:\n")
    if a!=l or not isdigit(b) or int(b)!=p:
        print("Sorry login-%s or password-%s is wrong"%(a,b)
    else:
        print("Welcom, {}".format(name))
